# Kod tabanı denetimi — raspberrypi

- Tarih: 2026-08-15
- Kapsam: /Users/batuhanyuksel/Documents/raspberrypi
- Runtime: static (`run.py`; `make-test` class=review, çalıştırılmadı)
- Verdict: CONCERNS
- Envanter: file_count=233, todo_count=0, docs=29, primary=shell
- Disk: Mac harici `disk40` (31.3GB SD bootfs+Linux), `disk41` (256.1GB YongzhenWeiye / JMS583, tek Linux partition)

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Major | Functional correctness | scripts/lib/ssd-alive.sh:22 |
| CA-002 | Major | Functional correctness | host/systemd/pi-gateway-health.timer |
| CA-003 | Major | Functional correctness | scripts/pi/ensure-ssd-fstab.sh:98 |
| CA-004 | Major | Functional correctness | scripts/lib/usb-quirk.sh |
| CA-005 | Major | Functional correctness | host/udev/99-pi-gateway-jmicron.rules |
| CA-006 | Major | Functional correctness | scripts/lib/ssd-alive.sh:197 |
| CA-007 | Major | Tests | scripts/mac/test-ssd-alive.sh:74 |
| CA-008 | Minor | Maintainability | scripts/lib/telegram-panels.py |
| CA-009 | Info | Security architecture | .env (ignored) |
| CA-010 | Info | Completeness | Makefile test target |

## CA-001 | Major | Functional correctness | scripts/lib/ssd-alive.sh:22

**Kanıt:** `SSD_USB_HOST_PORT` varsayılan `2`; `ssd_usb_port_sysfs` yalnız `usb2-port2`. Cihaz başka porta takılıysa `disable` sysfs yok → port cycle no-op, `ssd_usb_reset_record` çağrılmaz. YongzhenWeiye / JMS583 `152d:0583` Pi 4'te kopuyor [c6780b7bd8c8] [6075e73d35f2].
**Neden:** Soft-reset yanlış porta gidince JMicron bus dropout kurtarılamaz; stack degraded kalır.
**Yön:** Son bilinen portu kaydet; dropout'ta tüm `usbN-portM` disable cycle.

## CA-002 | Major | Functional correctness | host/systemd/pi-gateway-health.timer

**Kanıt:** Health timer `OnUnitActiveSec=2min`. `ssd-health.sh` yalnız `check-sd-health` / health-check zincirinde. `pi-ssd-watch.path` yalnızca `/dev/disk/by-label/pi-data`.
**Neden:** JMS583 flap saniyeler sürer; 2 dk kör pencere + stale by-label PathExistsGone kaçırır.
**Yön:** 30s `pi-ssd-health.timer` + udev `SYSTEMD_WANTS=pi-ssd-watch.service`.

## CA-003 | Major | Functional correctness | scripts/pi/ensure-ssd-fstab.sh:98

**Kanıt:** fstab `defaults,noatime,nofail` — `nodiscard` yok. JMS583 TRIM/UAS üzerinden UNMAP kopma üretir [9c5ab961425d].
**Neden:** Periyodik fstrim / discard, bridge'i resetler.
**Yön:** `nodiscard` zorunlu; mevcut satırı da güncelle.

## CA-004 | Major | Functional correctness | scripts/lib/usb-quirk.sh

**Kanıt:** SD `/Volumes/bootfs/cmdline.txt`: `usb-storage.quirks=152d:0583:u` var; `usbcore.quirks=…:k` (NO_LPM) ve `usbcore.autosuspend=-1` yok. `rootwait` / `rootfstype=ext4` çift. Kernel `k` = `USB_QUIRK_NO_LPM` [3aa9555fe875]; autosuspend=-1 [be7cb09dc67b].
**Neden:** UAS kapalı olsa da USB3 LPM JMS583 firmware'ini düşürür (Mac'te görünmez, Pi xHCI'de görünür).
**Yön:** cmdline'a `:k` + `autosuspend=-1`; helper tek kaynak.

## CA-005 | Major | Functional correctness | host/udev/99-pi-gateway-jmicron.rules

**Kanıt:** Kural yalnız `ATTR{power/control}=on` on add. `remove` tetikleyicisi yok. USB3 LPM sysfs yazılmıyor.
**Neden:** Kopma anında systemd handler geç gelir.
**Yön:** `ENV{ID_VENDOR_ID}` add/remove → `SYSTEMD_WANTS`; LPM U1/U2=0.

## CA-006 | Major | Functional correctness | scripts/lib/ssd-alive.sh#ssd_usb_reset_record

**Kanıt:** Aynı `ssd_usb_soft_reset` içinde port cycle `ssd_usb_reset_record` sonra xHCI yine record. `SSD_USB_RESET_MAX=3` iki flap'te biter.
**Neden:** Rate-limit bütçesi bir olayda iki kez düşer.
**Yön:** `ssd_usb_reset_record_once` per soft_reset.

## CA-007 | Major | Tests | scripts/mac/test-ssd-alive.sh:74

**Kanıt:** `grep COMPOSE_MODE="core-dns"` "SSH fail core-dns" sanıyor. Deploy artık `die "SSH compose mode probe failed"`. False green.
**Neden:** Kontrat eski bug'ı koruyor, yeni fail-closed'ı ölçmüyor.
**Yön:** SSH die string'ine bağla.

## CA-008 | Minor | Maintainability | scripts/lib/telegram-panels.py

**Kanıt:** `import-sample.py`: `sample: 0 edges, 1 files`, `orphans: telegram-panels.py`, `orphans_complete: true`.
**Neden:** Tek Python dosyası relative import grafında yalnız; shell çağırıyor olabilir. Ölü değil, graf kör.
**Yön:** Bilinçli; shell `python3` exec. Split yok.

## CA-009 | Info | Security architecture | .env

**Kanıt:** inventory `secret_candidates`: `.env` git=ignored; `.env.example` tracked placeholder; Caddy pem ignored.
**Neden:** Local secret normal. Tracked placeholder BLOCK değil.
**Yön:** Yok. SAST için ayrı `security-check`.

## CA-010 | Info | Completeness | Makefile

**Kanıt:** `runtime-check.py` `plans: [{kind: make-test, body: @./scripts/mac/validate.sh, class: review}]`, `runtime: static-only`.
**Neden:** Recipe `@./` class=review; `--runtime` yok, çalıştırılmadı. CLEAN iddiası yok.
**Yön:** Bu koşuda `make test` bilinçli çalıştırılmadı (skill).

## Disk (Mac, 2026-08-15)

| Disk | Kimlik | Boyut | Şema | Not |
|------|--------|-------|------|-----|
| SD | disk40 MassStorageClass | 31.3GB | MBR FAT32 `bootfs` + Linux | Hybrid OS; cmdline PARTUUID=`a49fcf66-02` |
| SSD | disk41 YongzhenWeiye | 256.1GB | MBR tek Linux | Veri diski, bootfs yok. SMART USB bridge'de yok |

ext4 superblock: Mac `sudo` şifre yok, okunamadı. SSD formatı Pi `setup-ssd-data` (label `pi-data`) beklenen.

## Önerilen sıra

1. CA-001 — USB port tarama + hatırlama (hardcoded port 2 kalksın)
2. CA-004 — cmdline NO_LPM + autosuspend (SD bootfs + helper)
3. CA-002 / CA-005 — 30s timer + udev hotplug
4. CA-003 / CA-006 — nodiscard + rate-limit session
5. CA-007 — deploy SSH kontratı

## Drift

İlk koşu (`previous: null`).
