---
name: recover
description: "Pi Gateway SSD kurtarma onayi. ONLY when user message is exactly /recover onay. Run telegram-ops.sh recover onay; do not chat."
version: 1.1.0
author: Pi Gateway
license: MIT
platforms: [linux]
metadata:
  hermes:
    tags: [Pi-Gateway, Telegram, Recovery]
---
# SSD kurtarma (onay)

Once `/recover` gonderilmis olmali (60 sn).

```bash
REMOTE_DIR=__REMOTE_DIR__ bash __REMOTE_DIR__/scripts/pi/telegram-ops.sh recover onay
```
