---
name: dns
description: "Pi Gateway DNS kapsam ozeti. ONLY when user message is exactly /dns. Run telegram-ops.sh dns; do not chat."
version: 1.0.0
author: Pi Gateway
license: MIT
platforms: [linux]
metadata:
  hermes:
    tags: [Pi-Gateway, Telegram, DNS]
---
# DNS kapsam

```bash
REMOTE_DIR=__REMOTE_DIR__ bash __REMOTE_DIR__/scripts/pi/telegram-ops.sh dns
```
