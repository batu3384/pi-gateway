# Kod tabanı denetimi — raspberrypi

- Tarih: 2026-08-16
- Kapsam: /Users/batuhanyuksel/Documents/raspberrypi
- Runtime: static
- Verdict: CONCERNS
- Envanter: file_count=271, todo_count=0 (inventory.py; `complete_todo_list: false`)
- Profil: primary=shell (150), python=1; import-sample: sample 0 edges, 1 files
- Odak: USB SSD kopma / port cycle / degraded FSM (canlı Pi: SSD enumerate yok, DNS ayakta)

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Major | Functional correctness | scripts/pi/health-check.sh:30 |
| CA-002 | Major | Architecture | scripts/lib/notify.sh:204 |
| CA-003 | Major | Architecture | scripts/lib/ssd-alive.sh:509 |
| CA-004 | Major | Architecture | scripts/lib/ssd-alive.sh:356 |
| CA-005 | Major | Completeness | host/systemd/pi-ssd-health.timer |
| CA-006 | Minor | Maintainability | scripts/lib/telegram-panels.py |
| CA-007 | Info | Security architecture | .env.example |
| CA-008 | Info | Tests | Makefile (runtime-check class: review) |

## CA-001 | Major | Functional correctness | scripts/pi/health-check.sh:30

**Kanıt:** `check-sd-health.sh` SSD yokken `ssd-health-fail` ile exit 1. `health-check.sh:30-32` bunu `sd_fail=1` yapar; `:254-255` her SSD degraded tick’te `exit 1`. `host/systemd/pi-gateway-health.service:4` `OnFailure=pi-gateway-health-failure.service`. Timer `OnUnitActiveSec=2min`. DNS core (unbound/adguard) ayrı ve sağlıklı olabilir. `docs/runbooks/SSD-FSM.md` degraded’i beklenen mod olarak tanımlar.

**Neden:** Beklenen FSM durumu (SSD yok, DNS SD üzerinde) systemd birimi fail sayılıyor. Operatör Telegram’da “SD kart değiştir / sağlık başarısız” görüyor; gerçek durum USB SSD enumerate değil.

**Yön:** `health-check` SSD degraded + `stack_dns_core_ok` iken exit 0. SSD kurtarma yalnız `pi-ssd-health.timer` / udev. `sd_fail` yalnız gerçek root RO / yazılamaz SD için.

## CA-002 | Major | Architecture | scripts/lib/notify.sh:204

**Kanıt:** `notify_health_systemd_fail` `notify_telegram` + key `health-systemd` kullanır (`:204-208`). `notify_telegram` (`:143-150`) `notify_rate_ok` ile `NOTIFY_COOLDOWN_SEC` (300). `notify_ssd_degraded` ise edge-trigger (`notify_send_with_transition`, `NOTIFY_REPEAT_SEC=3600`). `notify_sd_warn` (`:219-231`) metni “SD kart read-only… SD karti degistirin”; `check-sd-health.sh:74-76` `ssd-health-fail` bu yola (veya hâlâ exit 1 ile health-failure’a) bağlanır.

**Neden:** Aynı fiziksel olay iki kanal: (1) SD metni 5 dakikada bir, (2) “zamanlanmış sağlık” 5 dakikada bir. Edge-trigger SSD mesajı ile çakışır.

**Yön:** `OnFailure` SSD-only fail’de tetiklenmesin (CA-001). `notify_health_systemd_fail` edge-trigger + saatlik hatırlatma. `ssd-health-fail` asla `notify_sd_warn` olmasın.

## CA-003 | Major | Architecture | scripts/lib/ssd-alive.sh:509

**Kanıt:** `ssd_usb_soft_reset` (`:501-530`) lock `mkdir`; kilit varsa `return 1` (`:520-522`, log: `soft-reset zaten calisiyor`). Gövde port cycle `SSD_USB_PORT_OFF_SEC`+`ON_SEC` (5+10s) + settle. `health-check` 2 dk’da `SSD_HEALTH_AUTO=true` ile `ssd-health.sh` çağırır; `pi-ssd-health.timer` tasarımı 30s. İki oneshot üst üste binince ikinci tick sıfır iş yapar.

**Neden:** Kurtarma merdiveni kilitte yutulur. Canlı log bu satırı basıyor; port cycle bitmeden sonraki health tick “başarısız” sayılıyor.

**Yön:** Tek sahip: `pi-ssd-health.timer`. Health-check `SSD_HEALTH_AUTO=false` (gözlem). Kilit varsa bekle (timeout) veya “in-progress = henüz fail değil”.

## CA-004 | Major | Architecture | scripts/lib/ssd-alive.sh:356

**Kanıt:** `ssd_usb_port_disable_cycle` (`:340-367`) `ssd_usb_reset_record_once` **cycle’dan önce** (`:356`). `SSD_USB_RESET_MAX=3` / `WINDOW=900s` (`:13-14`). `SSD_USB_PORT_SCAN_MAX=1` (`:28`, `:347`). Aday sırası remembered → `SSD_USB_HOST_PORT` → diğer portlar (`:298-322`). Başarısız cycle da kotayı yakar. Kernel `usb2-port2: Cannot enable` ile aynı path cycle log’u (`:327`).

**Neden:** Ölü/yanlış hatırlanan port 3 denemede 15 dk sessizlik. Kablo gerçekten yoksa yazılım sonsuz cycle etmemeli; kota başarısız denemede tükenmemeli.

**Yön (doğru merdiven):** (1) remount, (2) usb-storage rebind (cihaz sysfs’te ise), (3) hatırlanan port bir kez, (4) kalan USB3 portlar (SCAN_MAX=1 kalksın, USB2’ye saçılmasın), (5) xhci yalnız opt-in, (6) N fail sonra dur, degraded kal, udev `add` restore. Kota: başarılı cycle veya exponential backoff — fail-first record değil. Fiziksel kopuk kablo yazılımla düzelmez; algoritma bunu kabul etmeli.

## CA-005 | Major | Completeness | host/systemd/pi-ssd-health.timer

**Kanıt:** Timer `OnUnitActiveSec=30s` (`pi-ssd-health.timer:6`). `ssd-health.service` `ExecStart=.../ssd-health.sh`. `bootstrap.sh:120,145,152` enable/start eder. `health-check.sh:29` yorum: “SSD gozcu: timer’da kurtarsin” — kod ise aynı tick’te `SSD_HEALTH_AUTO=true`. udev `99-pi-gateway-jmicron.rules:12-13` `SYSTEMD_WANTS=pi-ssd-watch.service` (add/remove). `pi-ssd-health.timer` Pi timer listesinde yokken recovery yalnız 2 dk health’e kalır.

**Neden:** Tasarlanan 30s poll ile çalışan yol ayrışmış. Health birimi hem gözcü hem kurtarıcı hem fail-alert.

**Yön:** Bootstrap sonrası `pi-ssd-health.timer` active doğrula (smoke). Health-check SSD recover etmesin.

## CA-006 | Minor | Maintainability | scripts/lib/telegram-panels.py

**Kanıt:** import-sample `orphans: ["scripts/lib/telegram-panels.py"]`, `orphans_complete: true`, sample: 0 edges, 1 files. inventory `top_by_lines[0]` 297 satır.

**Neden:** JS/py/go relative import grafında hiç import edilmiyor (shell çağrısı bu motorda görünmez).

**Yön:** Shell entrypoint varsa dokümante et; yoksa ölü kod say.

## CA-007 | Info | Security architecture | .env.example

**Kanıt:** inventory `secret_candidates`: `.env` git=ignored; `.env.example` git=tracked; `config/caddy/certs/home.pem` / `home-key.pem` ignored. Gövde okunmadı.

**Neden:** Tracked aday şablon dosya. Ignored `.env` BLOCK değil.

**Yön:** Şablon placeholder kalsın; gerçek sır commit etme.

## CA-008 | Info | Tests | Makefile

**Kanıt:** runtime-check `mode: static-only`, `plans[0].kind=make-test`, `class: review`, body `@./scripts/mac/validate.sh`. `--runtime` yok.

**Neden:** CLEAN ≠ çalışıyor. make çalıştırılmadı.

**Yön:** İstenirse `/codebase-audit --runtime` (class: safe plan yok; review çalışmaz).

## Önerilen sıra

1. CA-001 — Health-check SSD degraded iken DNS core OK ise exit 0; systemd fail olmasın.
2. CA-002 — Telegram: SSD kaybı tek edge mesaj; “SD kart değiştir” ve 2 dk’lık sağlık spam’i kalksın.
3. CA-003 — Soft-reset tek sahip (`pi-ssd-health.timer`); kilitte yutma.
4. CA-004 — Port cycle kotası fail’de tükenmesin; USB3 tarama; fiziksel yoksa dur.
5. CA-005 — `pi-ssd-health.timer` gerçekten aktif olsun; smoke ile doğrula.

## Drift

Önceki: 2026-08-16-0048.json
Yeni: 7 · Gitti: 9 · Aynı: 0

Yeni: Major Functional correctness `health-check.sh`; Major Architecture `notify.sh`, `ssd-alive.sh`; Major Completeness `pi-ssd-health.timer`; Minor `telegram-panels.py`; Info `.env.example`, `Makefile`.

Gitti: Major recover-readonly-root / setup-docker-ssd / test-adversarial-fixes; Minor prune / ssd-health / hotplug concurrency; Trivial ARCHITECTURE.md; Info `.env`.
