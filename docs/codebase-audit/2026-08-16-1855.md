# Kod tabanı denetimi — raspberrypi

- Tarih: 2026-08-16
- Kapsam: /Users/batuhanyuksel/Documents/raspberrypi (SSD recovery yazılım yolu — canlı olay 17:52–18:08)
- Runtime: static
- Sonuç: SORUNLU
- Envanter: file_count=275, todo_count=0 (`complete_todo_list: false`)
- Profil: primary=shell (150); import-sample: sample 0 edges, 1 files (`complete_graph: false`)

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Büyük | İşlev | scripts/pi/check-sd-health.sh:49 |
| CA-002 | Büyük | İşlev | scripts/lib/ssd-alive.sh:332 |
| CA-003 | Büyük | Mimari | scripts/lib/ssd-alive.sh:599 |
| CA-004 | Büyük | İşlev | scripts/pi/ssd-health.sh:29 |
| CA-005 | Büyük | Mimari | scripts/pi/ssd-hotplug-handler.sh:153 |
| CA-006 | Büyük | İşlev | scripts/lib/ssd-alive.sh:690 |
| CA-007 | Küçük | İşlev | scripts/pi/ssd-health.sh:57 |
| CA-008 | Küçük | Bakım | scripts/lib/telegram-panels.py |

## CA-001 | Büyük | İşlev | scripts/pi/check-sd-health.sh:49

**Kanıt:** `RECENT_KERNEL_ERRORS` grep `ext4.*(error|checksum)` ve genel `Buffer I/O error` içerir (`:49-54`, HEAD `367fe1d`). Canlı kernel: `EXT4-fs warning (device sdb1) … comm prometheus: error -5`. `ISSUES` hem `kernel-io-errors` hem `usb-ssd-disconnect` hem `ssd-health-fail` alır. `_ssd_only` yalnız `ssd-health-fail|usb-ssd-disconnect` sayar (`:123-128` HEAD); `kernel-io-errors` `_ssd_only=0` yapar → `notify_sd_warn` (`:140`) başlık **“SD Kart”** + “SD karti degistirin”. SD root yazılabilir (`touch` testi geçti).

**Neden:** Operatör SSD kopmasında SD kart ölümü görür. Algoritma FSM doğru çalışsa bile notify kanalı yanlış teşhis üretir — güven kaybı.

**Yön:** `kernel-io-errors` yalnız `mmcblk` / root FS. SSD `sdb` EXT4 + disconnect `usb-ssd-disconnect` / `notify_ssd_degraded` yolunda kalsın. Yerel fix var, **commit/deploy edilmemiş**.

## CA-002 | Büyük | İşlev | scripts/lib/ssd-alive.sh:332

**Kanıt:** `SSD_USB_RESET_MAX=3`, `WINDOW=900` (`:13-14`). `ssd_usb_reset_rate_limited` true iken `ssd_usb_port_disable_cycle` anında `return 1` (`:544-546`). `ssd_xhci_rebind` aynı gate (`:601-603`). Canlı journal 18:04: `[ssd-alive] port disable rate-limit (3/900s)`; ardından `[ssd-health] hala degraded — beklenen FSM (timer OK)`. `pi-ssd-health.timer` `OnUnitInactiveSec=30s` ile **30 sn’de bir** tetiklenmeye devam eder ama **15 dk boyunca sıfır recovery aksiyonu**.

**Neden:** “Yazılımla port kapat-aç” hedefi kota dolunca tamamen susar. Timer çalışıyor görünür; enumerate denemesi yok. Kullanıcı “algoritma neden durdu” der — kod tasarım hatası.

**Yön:** Kota yalnız başarısız **tam tur** sayılsın; bus dropout’ta xHCI ayrı bütçe veya rate-limit sonrası yavaş poll (port OFF yok, yalnız xhci/authorized). Enumerate/mount başarısında `ssd_usb_reset_clear` (kısmen var) her kopma sonrası da.

## CA-003 | Büyük | Mimari | scripts/lib/ssd-alive.sh:599

**Kanıt:** `ssd_xhci_rebind` `SSD_USB_XHCI_REBIND:-false` ile kapalı (`:599-600`). Merdiven sırası: port cycle → **opt-in** xhci (`:659-665`). `docs/SSD-JMICRON-FIX.md:67` adım 5 opt-in. Canlı: bus dropout (`lsusb` yalnız hub), port cycle 17:57–18:00 başarısız; xhci **otomatik çalışmadı**. Manuel `SSD_USB_XHCI_REBIND=true` ile xhci denendi (18:08) — yine `Cannot enable`.

**Neden:** Total bus dropout senaryosunda yazılımın son host-controller kartı varsayılan kapalı. Dokümantasyon “yazılım yolu” der; kod dropout’ta xhci’yi hiç denemez. Bu olayda xhci de kurtarmadı ama **otomatik merdiven eksik** — hata “kablo değiştir”e kaymadan önce kod kendi tavanını tam denememiş.

**Yön:** Bus dropout + `ssd_usb_bus_dropout` true iken xhci’yi rate-limit’ten bağımsız veya ayrı slot ile dene (collateral kabul). Veya `.env` default `SSD_USB_XHCI_REBIND=true` hybrid JMicron profilde.

## CA-004 | Büyük | İşlev | scripts/pi/ssd-health.sh:29

**Kanıt:** `ssd_under_voltage` `vcgencmd get_throttled` bit0/bit16 (`ssd-alive.sh:784-791`). `ssd-health.sh:29-30` yalnız `log WARN`. Canlı `throttled=0xe0000` (under-voltage geçmişi). Port cycle OFF+ON 5+10s × çok port devam etti (`journal` 17:57–18:00).

**Neden:** Düşük güçte agresif USB port power-cycle enumerate’i **bozar**. Kod undervolt’u görüp davranış değiştirmiyor — aynı anda hem “güç yetersiz” hem “portu kapat aç”.

**Yön:** Undervolt aktif/geçmişse port disable atla; yalnız remount/rebind veya bekle + notify. Güç stabil olunca cycle.

## CA-005 | Büyük | Mimari | scripts/pi/ssd-hotplug-handler.sh:153

**Kanıt:** Degraded geçişte `docker compose stop` listesi: `n8n forgejo syncthing uptime-kuma crowdsec redis dozzle netalertx` (`:153-154`). **prometheus, grafana, node-exporter yok.** `recover-compose-up.sh` core-dns yalnız `unbound adguard homepage caddy` (`:34`). Kernel 17:58–18:05: `comm prometheus` + `device sdb1` EXT4 hataları. `data/prometheus` SSD symlink altında (`ensure-monitoring-data.sh:9`).

**Neden:** SSD kopunca monitoring container’ları durmazsa ölü `sdb1` ghost block’a I/O basar — journal spam, yanlış `kernel-io-errors`, FS daha kötü.

**Yön:** Degraded `stop` listesine `prometheus grafana node-exporter` ekle. Ghost mount sonrası monitoring profile kapalı kalsın.

## CA-006 | Büyük | İşlev | scripts/lib/ssd-alive.sh:690

**Kanıt:** `_ssd_usb_soft_reset_body` bus dropout sonrası `ssd_block_present` true ise `partprobe` çağırır (`:690-695`). `ssd_block_present` disconnect sonrası stale `sdb` node’u görebilir. Disconnect sonrası `echo 1 > /sys/block/sdX/device/delete` veya benzeri **yok**.

**Neden:** Ölü block üzerinde partprobe/probe I/O hata üretir; recovery’yi zorlaştırır. Yazılım disconnect’i kernel’den temizlemiyor.

**Yön:** `ssd_usb_bus_dropout` + block var ama sysfs yok → güvenli `device/delete` + lazy umount. partprobe yalnız canlı enumerate sonrası.

## CA-007 | Küçük | İşlev | scripts/pi/ssd-health.sh:57

**Kanıt:** Degraded dalında recovery başarısız → `log "hala degraded — beklenen FSM (timer OK)"` + **`exit 0`** (`:57-58`). Rate-limit veya xhci skip durumunda da aynı. systemd unit **success** sayılır; operatör journal’da “OK” ile “degraded” karışır.

**Neden:** Gözlemlenebilirlik — failure masked.

**Yön:** Rate-limit / dropout’ta `exit 0` kalabilir ama log seviyesi `WARN` + `rate-limited no-op` açık metin; isteğe bağlı `logger` metric.

## CA-008 | Küçük | Bakım | scripts/lib/telegram-panels.py

**Kanıt:** import-sample `orphans: ["scripts/lib/telegram-panels.py"]`, `orphans_complete: true`, sample: 0 edges, 1 files. `telegram-bot.sh` shell ile çağırır.

**Neden:** Graf motoru orphan; bakım gürültüsü.

**Yön:** Dokümante shell entrypoint; orphan scope dışı.

## Önerilen sıra

1. CA-001 — SSD `sdb1` I/O’yu SD Telegram’ından ayır; fix commit + `install-privileged-scripts`.
2. CA-005 — Degraded’de prometheus/grafana durdur; ghost I/O kes.
3. CA-002 — Rate-limit sonrası 15 dk susma; bus dropout’ta alternatif poll.
4. CA-003 — JMicron profilde xhci merdiven adımını otomatik veya opt-out değil opt-in açık yap.
5. CA-004 — Undervolt’ta port power-cycle yasak; güç mesajı.

## Drift

Önceki: `2026-08-16-1655.json`  
Yeni: 6 · Gitti: 2 · Aynı: 2

**Yeni**
- Büyük İşlev — scripts/pi/check-sd-health.sh (SSD→SD notify, olay odaklı)
- Büyük İşlev — scripts/lib/ssd-alive.sh (rate-limit dead zone)
- Büyük Mimari — scripts/lib/ssd-alive.sh (xhci default kapalı)
- Büyük İşlev — scripts/pi/ssd-health.sh (undervolt yalnız WARN)
- Büyük Mimari — scripts/pi/ssd-hotplug-handler.sh (monitoring stop eksik)
- Büyük İşlev — scripts/lib/ssd-alive.sh (ghost block partprobe)

**Gitti**
- Büyük Mimari — scripts/lib/notify.sh (health-systemd; önceki koşuda ele alındı)
- Büyük Mimari — host/systemd/pi-ssd-health.timer (OnUnitInactiveSec fix merge’de)

## Canlı olay özeti (yazılım perspektifi)

Algoritma **çalıştı** (port tarama 17:57, kota 18:04, timer 30s). **Enumerate olmadı** — kernel `Cannot enable`. Asıl yazılım hataları: (1) yanlış Telegram teşhisi, (2) kota sonrası tam susma, (3) xhci varsayılan kapalı, (4) undervolt yok sayıldı, (5) prometheus ghost I/O. Bunlar “kablo değiştir”den önce düzeltilmeli.
