# Kod tabanı denetimi — raspberrypi

- Tarih: 2026-08-25
- Kapsam: `/Users/batuhanyuksel/Documents/raspberrypi`
- Ölçüm: 1.3.6 / 3137c66be8ef5fe5 (`run.py` stdout)
- Runtime: static
- Sonuç: SORUNLU
- Envanter: file_count=314, todo_count=0, diller=shell(173)+python(6), tarama complete=true

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Büyük | İşlev | scripts/lib/hermes-cron-patch.py:10 |
| CA-002 | Büyük | İşlev | scripts/lib/bulletin-slo.py:43 |
| CA-003 | Küçük | Belgeler | wiki/FAQ.md |
| CA-004 | Küçük | Belgeler | docs/README-INSTALL-DOCTOR-design.md |
| CA-005 | Bilgi | Bakım | scripts/lib/*.py (import-sample orphans) |

## CA-001 | Büyük | İşlev | scripts/lib/hermes-cron-patch.py:10

**Kanıt:** v5 timeout/Firecrawl/400/429 şablonları `no_agent` dalından önce. Watchdog/NetAlert/piyasa timeout’u “bülten üretilemedi + 18:55” der. Pi’de v5 zaten basılı; gövde değişince marker aynı kalırsa yeniden yama yok.
**Neden:** Agent-olmayan görev model çağırmaz. Yanlış şablon operatörü yanlış yere bakıtır.
**Yön:** Agent hatalarını `if not job.get("no_agent")` içine al. Marker’ı v6 yap (Pi’deki v5’i değiştirir). Bu koşuda v6 yazıldı ve test edildi.

## CA-002 | Büyük | İşlev | scripts/lib/bulletin-slo.py:43

**Kanıt:** `last_run` yoksa `continue`. 07/19/23 hiç ateşlenmezse `bulletin-stale:*` çıkmaz.
**Neden:** SLO “son başarı eski” bakıyor, “hiç çalışmadı” bakmıyor. Scheduler kırığı sessiz kalır.
**Yön:** `created_at` > 26s ve `last_run` yoksa `bulletin-stale:ad(never)`. `created_at` de yoksa never (eski kayıt). Yeni spawn `created_at` yazar. Bu koşuda eklendi.

## CA-003 | Küçük | Belgeler | wiki/FAQ.md

**Durum:** Kapatıldı — wiki iç linkler `Page.md` (`wiki/README.md` kuralı).
**Eski kanıt:** docs-check uzantısız Gollum href’i dosya sanırdı.

## CA-004 | Küçük | Belgeler | docs/README-INSTALL-DOCTOR-design.md

**Durum:** Kapatıldı — `INSTALL.md` + `scripts/mac/doctor.sh` + `make doctor` var; tasarım **Implemented**. `KURULUM.md` kasıtlı yok.

## CA-005 | Bilgi | Bakım | scripts/lib/*.py

**Kanıt:** import-sample 6 “orphan”: bulletin-slo, hermes-cron-merge/patch, hermes-gateway-patch, netalert-devices, telegram-panels. Relative import yok.
**Neden:** CLI script; `python3 scripts/lib/....py`. Graf ölü kod demek değil.
**Yön:** `__main__` girişleri duruyor. Paketleştirme YAGNI.

## Önerilen sıra

1. CA-001 — v6 yama Pi’ye bas (timeout `no_agent` değil). Kod hazır; deploy bu koşunun parçası.
2. CA-002 — never-run: `created_at` yoksa da `bulletin-stale:ad(never)`.
3. CA-003/004 — kapatıldı (wiki `.md`, install-doctor Implemented).
4. CA-005 — yok (bilinçli CLI).

## Drift

Önceki: 2026-08-16-1855.json
Yeni: 5 · Gitti: 7 · Aynı: 0

Gitti (SSD/SD health Majors — önceki koşu). Yeni: Hermes v5 timeout/SLO + wiki/docs.

## Ölçüm notu

- `secret_candidates`: `.env`, `.env.bak`, caddy certs — git=ignored, tracked değil.
- `stub-scan` hit_count=0.
- `runtime-check` mode=static; Makefile validate.sh review-class, çalıştırılmadı.
- Bu koşuda CA-001/CA-002/CA-003/CA-004 kapatıldı (v6 + never-run + wiki `.md` + doctor). Pi deploy ayrı adım.
